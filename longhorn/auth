#USER="darmond"; PASSWORD="darmond"; echo "${USER}:$(openssl passwd -stdin -apr1 <<< ${PASSWORD})" >> auth
#kubectl -n longhorn-system create secret generic basic-auth --from-file=auth

darmond:$apr1$h80OhE0L$fIZ8QwvHw4dp1hNB5PX1R/
